# IBA 데이터 분석 필터 설정 현황 및 조정 요청서

**작성일**: 2026-02-09
**목적**: 현재 적용 중인 필터 설정에 대한 항목별 설명 및 기준값 조정 검토 요청

---

## 1. 필터 시스템 개요

현재 IBA 데이터 이상치 분석 시 **정상 공정 상태의 데이터만 분석에 포함**하기 위해 4종의 필터를 단계적으로 적용하고 있습니다. 각 필터는 설비 특성에 따라 카테고리별로 선택 적용되며, 비정상 구간(비가동, 롤 교환, 권취 과도기 등)의 데이터를 제외하여 분석 신뢰도를 확보하는 것이 목적입니다.

---

## 2. 필터별 상세 설명

### 2.1 RUN 필터 (`run_only`)

| 항목 | 내용 |
|------|------|
| **목적** | 정상 압연 가동 중 데이터만 분석 대상으로 선별 |
| **적용 조건** | `OPERATION_STATUS = 'RUN'` |
| **제외 대상** | IDLE(정지/대기), TRANS(전환/시작·종료) 상태의 데이터 |
| **적용 범위** | **전체 85개 태그 (100%)** |
| **데이터 영향** | 전체 데이터의 약 **44.6%만 선택** (나머지 55.4% 제외) |

**적용 근거**: IDLE 상태는 설비 미가동 상태이므로 센서 값이 공정 특성을 반영하지 않습니다. RUN 상태에서만 유의미한 공정 데이터가 생성되므로, 모든 태그에 기본 적용합니다.

**확인 요청 사항**:
- [ ] IDLE/TRANS 상태 제외에 동의하십니까?
- [ ] TRANS(전환) 상태의 데이터를 별도 분석 대상으로 포함해야 하는 경우가 있습니까? (예: 압연 시작/종료 시점의 과도 상태 분석)

---

### 2.2 Special-Ops 필터 (`special_ops`)

| 항목 | 내용 |
|------|------|
| **목적** | 특수 작업 구간(비정상 가동)의 데이터 제외 |
| **적용 조건** | `MILL_STATUS = 'RUN'` **AND** `FURNACE_STATUS = 'HOT'` |
| **제외 대상** | 압연기 비가동(MILL ≠ RUN) 또는 가열로 저온(FURNACE ≠ HOT) 상태 |
| **적용 범위** | **전체 85개 태그 (100%)** |
| **데이터 영향** | RUN 데이터 중 약 **90%만 선택** (특수 작업 구간 ~10% 제외) |

**적용 근거**: 가열로가 HOT 상태가 아니면 빌렛이 정상 온도에 도달하지 않은 상태이며, 압연기가 RUN이 아니면 스탠드 데이터에 공정 의미가 없습니다. 두 조건을 동시에 만족해야 정상 공정으로 판단합니다.

**확인 요청 사항**:
- [ ] MILL_STATUS, FURNACE_STATUS 두 조건의 동시 적용에 동의하십니까?
- [ ] `FURNACE_STATUS = 'HOT'`의 판정 기준이 현재 시스템 설정과 일치합니까?
- [ ] 가열로 승온 중(WARM → HOT 전환 구간)의 데이터도 제외하는 것이 적절합니까?

---

### 2.3 Roll-Change 필터 (`roll_change`)

| 항목 | 내용 |
|------|------|
| **목적** | 롤 교환 및 롤 직경 변경 시 발생하는 비정상 데이터 제외 |
| **적용 조건 1** | `OPERATION_STATUS ≠ 'TRANS'` (전환 상태 제외) |
| **적용 조건 2** | **속도 변화율 < 50%** (`speed_spike_threshold = 0.5`) |
| **기준 태그** | `STAND_1_ACTUAL_SPEED` (1번 스탠드 실제 속도) |
| **적용 범위** | **64개 태그 (75.3%)** — 스탠드(05~07) + 핀치롤(08, 10) |
| **미적용 범위** | 가열로(01~04) + 냉각수(09) = 21개 태그 |
| **데이터 영향** | 적용 태그에서 약 **11% 추가 제외** |

**적용 근거**: 롤 교환 시 스탠드 속도·토크·부하가 급격히 변화하며, 이 구간의 데이터는 정상 공정 범위를 벗어납니다. 가열로 태그(01~04)는 열관성이 크므로 롤 교환의 영향을 받지 않아 미적용합니다.

**현재 임계값**:

| 파라미터 | 현재 값 | 의미 |
|----------|---------|------|
| `speed_spike_threshold` | **0.5 (50%)** | 직전 대비 50% 이상 속도 변화 시 롤 교환으로 판정 |

**확인 요청 사항**:
- [ ] 롤 교환 감지 기준인 **50% 속도 변화율**이 실제 롤 교환 패턴에 부합합니까?
  - 너무 높으면: 일부 롤 교환 구간이 필터링되지 않음 (미탐지)
  - 너무 낮으면: 정상 속도 변동까지 롤 교환으로 오탐지
- [ ] 1번 스탠드(`STAND_1_ACTUAL_SPEED`)를 기준 태그로 사용하는 것이 적절합니까? 다른 스탠드가 더 적합한 경우가 있습니까?
- [ ] 피니싱 블록은 현재 **30%** 임계값을 별도 적용 중입니다(`FINISHING_BLOCK_ACTUAL_SPEED`). 이 차별 적용이 적절합니까?
- [ ] 가열로 태그(01~04)의 롤 교환 필터 미적용에 동의하십니까?

---

### 2.4 Coiling 필터 (`coiling_transient`)

| 항목 | 내용 |
|------|------|
| **목적** | 권취 시작/종료 시 가속·감속 구간의 과도 데이터 제외 |
| **적용 조건** | **속도 변화율 ≤ 5%** 구간만 정속으로 판정 (`coiling_steady_threshold = 0.05`) |
| **기준 태그** | `PINCHROLL_2_ACTUAL_SPEED` (2번 핀치롤 실제 속도) |
| **이동평균 윈도우** | **5초** (`coiling_window = 5`) |
| **적용 범위** | **18개 태그 (21.2%)** — 핀치롤(08) + PR상세(10)만 |
| **미적용 범위** | 가열로(01~04), 스탠드(05~07), 냉각수(09) = 67개 태그 |
| **데이터 영향** | 적용 태그에서 약 **15~25% 추가 제외** |

**적용 근거**: 권취 시작(COIL_START) 시 핀치롤이 급가속하고, 종료(COIL_END) 시 급감속합니다. 이 과도 구간의 토크·속도 값은 정상 공정 범위를 크게 벗어나므로, 정속 권취 구간만 분석에 포함합니다.

**현재 임계값**:

| 파라미터 | 현재 값 | 의미 |
|----------|---------|------|
| `coiling_steady_threshold` | **0.05 (5%)** | 이동평균 기준 5% 이내 변화율 시 정속 판정 |
| `coiling_window` | **5초** | 속도 변화율 계산을 위한 이동평균 윈도우 |

**확인 요청 사항**:
- [ ] 정속 구간 판정 기준 **5% 변화율**이 적절합니까?
  - 너무 높으면: 가감속 구간이 정속으로 오판되어 노이즈 포함
  - 너무 낮으면: 정상 미세 변동까지 제외하여 분석 데이터 과다 손실
- [ ] 이동평균 윈도우 **5초**가 권취 과도 구간의 특성에 부합합니까?
  - 짧으면: 순간 변동에 민감 (노이즈에 취약)
  - 길면: 실제 가감속 구간 포착이 느려짐
- [ ] 2번 핀치롤(`PINCHROLL_2_ACTUAL_SPEED`)을 기준 태그로 사용하는 것이 적절합니까?
- [ ] 스탠드 태그(05~07)에 Coiling 필터를 적용하지 않는 것에 동의하십니까? (스탠드는 권취와 물리적으로 이격되어 영향이 제한적이라 판단)

---

### 2.5 A/B 라인 필터 (PR_Detailed 전용)

| 항목 | 내용 |
|------|------|
| **목적** | PR_Detailed(Cat 10) 태그의 L1/L2 라인별 독립 분석 보장 |
| **적용 조건** | 태그명에서 L1/L2 추출 → DB `line` 컬럼으로 해당 라인 데이터만 선별 |
| **DB 매핑** | `line = 'A'` → L1 라인, `line = 'B'` → L2 라인 |
| **적용 범위** | **6개 태그 (7.1%)** — PR_Detailed(Cat 10)만 |
| **미적용 범위** | Cat 01~09 (79개 태그) — L1/L2 분기점 이전 공유 설비 |
| **폴백** | 필터 후 데이터 < 10건 시 0값 제외 방식으로 대체 |

**적용 근거**: VCC 권취 영역은 A라인(L1)과 B라인(L2)이 교대 운전됩니다. 한쪽 라인이 가동 중일 때 반대 라인의 센서는 0값을 출력하므로, `OPERATION_STATUS = 'RUN'` 필터만으로는 비활성 라인의 0값을 걸러낼 수 없습니다. DB `line` 컬럼 기반으로 해당 라인의 데이터만 추출해야 정확한 분석이 가능합니다.

**대상 태그**:

| 태그명 | 추출 라인 | DB 필터 |
|--------|:---------:|:-------:|
| PR6L1_ACT_TORQUE | L1 | `line = 'A'` |
| PR6L2_ACT_TORQUE | L2 | `line = 'B'` |
| PR7L1_ACT_TORQUE | L1 | `line = 'A'` |
| PR7L2_ACT_TORQUE | L2 | `line = 'B'` |
| PR8L1_ACT_TORQUE | L1 | `line = 'A'` |
| PR9L1_ACT_TORQUE | L1 | `line = 'A'` |

**확인 요청 사항**:
- [ ] DB `line` 컬럼의 `'A'` = L1, `'B'` = L2 매핑이 정확합니까?
- [ ] 향후 L2 태그(PR8L2, PR9L2)가 추가 수집될 경우, 동일한 필터링 로직이 자동 적용됩니다. 이에 동의하십니까?

---

## 3. 카테고리별 필터 적용 현황 요약

| 카테고리 | 태그 수 | RUN | Special-Ops | Roll-Change | Coiling | A/B 라인 | 적용 근거 |
|----------|:------:|:---:|:-----------:|:-----------:|:-------:|:--------:|-----------|
| 01 가열로 상부 온도 | 4 | O | O | **X** | **X** | **X** | 열관성이 커서 후공정 영향 없음 |
| 02 가열로 하부 온도 | 4 | O | O | **X** | **X** | **X** | 열관성이 커서 후공정 영향 없음 |
| 03 추출 온도 | 1 | O | O | **X** | **X** | **X** | 압연 전 측정이므로 롤교환 무관 |
| 04 가열로 부대설비 | 10 | O | O | **X** | **X** | **X** | 독립 제어 시스템 |
| 05 스탠드 토크 | 16 | O | O | O | **X** | **X** | 롤교환 시 토크 급변 |
| 06 스탠드 속도 | 15 | O | O | O | **X** | **X** | 롤교환 감지 기준 태그 |
| 07 스탠드 부하 | 15 | O | O | O | **X** | **X** | 토크·속도 함수 |
| 08 핀치롤 | 12 | O | O | O | O | **X** | 권취 직접 연동 |
| 09 냉각수 | 2 | O | O | **X** | **X** | **X** | 독립 냉각 시스템 |
| 10 PR 상세 (L1/L2) | 6 | O | O | O | O | **O** | 권취기 직전, A/B 라인 교대 운전 |

---

## 4. 조정 검토 요청 항목 정리

| # | 필터 | 파라미터 | 현재 값 | 검토 요청 |
|---|------|----------|---------|-----------|
| 1 | Roll-Change | `speed_spike_threshold` | **50%** | 실제 롤 교환 시 속도 변화 패턴에 부합하는지 확인 |
| 2 | Roll-Change | 기준 태그 | `STAND_1_ACTUAL_SPEED` | 가장 대표적인 기준 태그인지 확인 |
| 3 | Roll-Change | 피니싱 블록 임계값 | **30%** (별도 적용) | 피니싱 블록 차별 임계값의 적절성 |
| 4 | Coiling | `coiling_steady_threshold` | **5%** | 정속 구간 판정 기준의 적절성 |
| 5 | Coiling | `coiling_window` | **5초** | 이동평균 윈도우 크기의 적절성 |
| 6 | Coiling | 기준 태그 | `PINCHROLL_2_ACTUAL_SPEED` | 가장 대표적인 기준 태그인지 확인 |
| 7 | Special-Ops | FURNACE_STATUS | `HOT` | HOT 판정 기준과 시스템 설정 일치 여부 |
| 8 | RUN | TRANS 상태 | 전체 제외 | TRANS 상태 별도 분석 필요 여부 |
| 9 | A/B 라인 | DB `line` 매핑 | A=L1, B=L2 | DB `line` 컬럼의 A/B 값이 L1/L2 라인에 정확히 대응하는지 확인 |
| 10 | A/B 라인 | 폴백 임계값 | 10건 미만 | 라인 필터 후 데이터 부족 시 0값 제외 방식으로 대체하는 기준(10건)의 적절성 |

> 상기 항목에 대해 현장 운영 경험을 바탕으로 기준값 조정이 필요한 경우 알려주시면, 필터 설정에 즉시 반영하여 재분석을 진행하겠습니다. 코드 수정 없이 설정 파일(`config/tag_filter_config.yaml`)만 변경하면 전 분석 기법에 일괄 적용됩니다.
